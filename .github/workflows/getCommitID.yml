name: Get Commit ID
on:
    push:
        branches: feature/GetCommitID
    workflow_dispatch:
jobs:
    Install_Packages:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2
              with:
                fetch-depth: '0'
            - name: Fetch latest SHA
              run: |
                  echo $(git branch --show-current) >> ${{secrets.WorkingBranch}}
                  git checkout main
                  echo $(git rev-parse HEAD) >> ${{secrets.COMMIT_ID}}
                  git checkout ${{secrets.WorkingBranch}}
